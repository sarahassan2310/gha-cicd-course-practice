name: Pattern A - Matrix From Secret

on:
  workflow_dispatch:

jobs:
  matrix-job:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        version: ${{ fromJSON(secrets.VERSIONS) }}

    steps:
      - name: Print version
        run: echo "Running version ${{ matrix.version }}"